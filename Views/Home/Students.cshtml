@model IEnumerable<U20431644_H05.Models.Students>
@{
    ViewData["Title"] = "Students";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Students</h1>

<div class="text-center">
    <h1 class="display-4">Courses</h1>

    @if (@Model.Count() == 0)
    {
        <p>No Courses</p>
    }
    else
    {
        @* <ul class="list-group">
    <li class="list-group-item">
    @foreach (var students in Model)
    {
    <div class="card mb-3">
    <div class="card-body">
    <h5 class="card-title">"Student name "@students.Name</h5>
    <p class="card-text">@students.StudentId</p>
    @* @Html.ActionLink("View assignments", "ViewCourse", "Home", new { courseID = @course.ID } , new { @class = "btn btn-primary" })
    </div>
    </div>
    }
    </li>
    </ul>*@

        <table class="table">
            <tr>
                <th>
                    Students
                </th>
            </tr>
            @foreach (var students in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => students.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => students.Surname)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => students.Sclass)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => students.point)
                    </td>
                    <td>
                        <button>Borrow Book</button>
                    </td>
                </tr>
            }
        </table>

    }
</div>



